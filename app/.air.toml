# .air.toml
root = "../../../"
tmp_dir = "tmp"

[build]
  cmd = "go build -o tmp/main ./cmd/api"
  bin = "tmp/main"
  full_bin = "false"
  delay = 1000
  log = "build.log"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor"]

[log]
  time = true
